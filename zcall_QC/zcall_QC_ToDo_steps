Link to data _Matrix_plink_cleaned
link to threshold folder in output folder
run RunAllCalibrate with:
RunAlleCalibrate PATH DATAFILE
Path ('root' of zcall_QC or just . when standing here)
Datafile (use the link)
It will create the concordance folder and a lot of qsub outs.
Look through the results, find the best and use it with zCall:
zCall -R Final_Report_Matrix_plink_cleaned (link) -T Chosen_Threshold -O Output_Folder/plink_name
update alleles and redo geno with plink (while in the output dir):
plink --noweb --tfile plink_name --update-alleles ../../scripted_QC/alleles.list --geno 0.05 --make-bed --out zcall_core
Extract, from MatrixMissing.lmiss, all the snps that have to high a missing rate and sort them out. You can't use plinks geno after zcall.
use in scriptedQC/output/
awk '$5>0.05 {print $2}' MatrixMissing.lmiss > ../../zcall_QC/output/missing_snps_from_scriptedQC
then
plink --noweb --bfile zcall_core --exclude missing_snps_from_scriptedQC --make-bed --out zcall_core_clean